# GPT-DB Accelerator Module

Building across multiple platforms and hardware is complex, and the GPT-DB Accelerator aims to provide compatibility handling for this, offering as consistent an interface as possible for other core models.

